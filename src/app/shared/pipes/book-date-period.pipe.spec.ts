import { BookDatePeriodPipe } from './book-date-period.pipe';

describe('BookDatePeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new BookDatePeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
